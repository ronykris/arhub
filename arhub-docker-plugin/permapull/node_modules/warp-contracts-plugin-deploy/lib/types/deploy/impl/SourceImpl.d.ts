import { Warp, ArWallet, CustomSignature, Source, SourceData, Transaction } from 'warp-contracts';
import { DataItem, Signer } from 'warp-arbundles';
export declare class SourceImpl implements Source {
    private readonly warp;
    private readonly logger;
    private signature;
    constructor(warp: Warp);
    createSource(sourceData: SourceData, wallet: ArWallet | CustomSignature | Signer, disableBundling?: boolean): Promise<DataItem | Transaction>;
    saveSource(src: DataItem | Transaction, disableBundling?: boolean): Promise<string>;
    private postSource;
    private createSourceArweave;
    private createSourceBundlr;
}
//# sourceMappingURL=SourceImpl.d.ts.map